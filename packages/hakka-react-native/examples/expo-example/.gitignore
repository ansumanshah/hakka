# Regenerated by `expo prebuild` — the whole point of this example is
# exercising the hakka-react-native config plugin against a clean prebuild,
# so the native projects it produces are not committed.
ios/
android/

# Expo
.expo/
dist/
web-build/

# Dependencies
node_modules/

# System
.DS_Store

# Metro/TS
*.tsbuildinfo
